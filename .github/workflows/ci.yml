name: CI Sanity Check

on:
  push:
  workflow_dispatch:

jobs:
  sanity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Python sanity test
        run: |
          python - <<'PY'
import os
print("Python ran successfully")
print("PWD:", os.getcwd())
PY
